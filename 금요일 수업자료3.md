# 1. Waypoint 순차 주행 — Sport API 직접 사용

이번 실습에서는 Go2의 **Sport Client API**를 직접 사용하여 정해진 순서대로 이동하는 Waypoint 주행을 구현합니다.

이번 예제의 동작 순서는 다음과 같습니다.

```text
기립
→ 전진
→ 정지
→ 좌회전
→ 정지
→ 전진
→ 정지
→ 우회전
→ 정지
→ 전진
→ 최종 정지
```

이번 Waypoint는 실제 위치 좌표를 이용하여 목표점을 찾아가는 자율주행 방식이 아닙니다.

```text
정해진 속도
+
정해진 시간
```

을 순서대로 실행하여 경로를 만드는 **시간 기반 Waypoint 주행**입니다.

이번에는 `/cmd_vel`을 발행하지 않고 Sport API를 직접 호출합니다.

```text
student_waypoint_sport.cpp
        ↓
SportClient::Move()
        ↓
Unitree Sport API
        ↓
       Go2
```

따라서 이번 실습에서는 `go2_nav_bridge`를 실행하지 않습니다.

---

## 1.1 먼저 생각해 보기 — 어떤 기능이 필요할까?

완성된 코드를 작성하기 전에 Waypoint 주행에 필요한 동작을 먼저 나누어 봅니다.

우리가 만들고 싶은 동작은 다음과 같습니다.

```text
1. 로봇을 일으킨다.
2. 일정 시간 동안 전진한다.
3. 멈춘다.
4. 일정 시간 동안 회전한다.
5. 멈춘다.
6. 같은 동작을 조합하여 여러 구간을 이동한다.
7. 모든 주행이 끝나면 반드시 정지한다.
```

그렇다면 어떤 Sport API 함수가 필요할지 먼저 생각해 봅시다.

### 생각 ① 로봇을 일으키려면?

Sport API에서는 다음 함수를 사용할 수 있습니다.

```cpp
sport_client_.StandUp(req_);
```

`StandUp()`은 Go2를 기립시키는 명령입니다.

Waypoint 주행을 시작하기 전에 먼저 기립이 완료되어야 합니다.

---

### 생각 ② 앞으로 움직이려면?

Go2의 이동 명령은 다음과 같습니다.

```cpp
sport_client_.Move(req_, vx, vy, vyaw);
```

세 값의 의미는 다음과 같습니다.

```text
vx
→ 전후 이동 속도

vy
→ 좌우 이동 속도

vyaw
→ 회전 각속도
```

예를 들어 다음 코드는 전방으로 `0.2 m/s` 속도로 이동하라는 의미입니다.

```cpp
sport_client_.Move(
  req_,
  0.2f,
  0.0f,
  0.0f
);
```

---

### 생각 ③ 왼쪽과 오른쪽 회전은 어떻게 구분할까?

전진 속도를 0으로 두고 `vyaw`에 값을 넣으면 제자리에서 회전할 수 있습니다.

왼쪽 회전:

```cpp
sport_client_.Move(
  req_,
  0.0f,
  0.0f,
  0.5f
);
```

오른쪽 회전:

```cpp
sport_client_.Move(
  req_,
  0.0f,
  0.0f,
  -0.5f
);
```

즉 회전 방향은 `vyaw`의 **부호**로 구분합니다.

---

### 생각 ④ 1초 동안만 움직이게 하려면?

`Move()` 함수는 이동 속도를 지정하는 함수입니다.

그렇다면 다음과 같은 기능이 필요합니다.

```text
Move 명령 전송
→ 일정 시간 동안 유지
→ StopMove
```

이번 실습에서는 이동 명령을 약 10 Hz로 반복해서 보내는 다음 함수를 직접 만들어 사용합니다.

```cpp
move_for(
  vx,
  vy,
  vyaw,
  duration
);
```

예를 들어:

```cpp
move_for(
  0.2f,
  0.0f,
  0.0f,
  1.0
);
```

은 다음과 같은 의미입니다.

```text
전방 0.2 m/s 명령을
약 10 Hz로 반복 전송
        ↓
1초 동안 유지
        ↓
StopMove()
```

---

### 생각 ⑤ 왜 매 구간마다 StopMove가 필요할까?

전진이 끝난 직후 회전을 시작한다고 생각해 봅시다.

다음처럼 각 동작 사이를 명확하게 끊어 주면 경로를 이해하고 디버깅하기 쉽습니다.

```text
전진
→ StopMove
→ 회전
→ StopMove
→ 전진
```

또한 프로그램의 마지막에도 반드시 다음 정지 명령을 호출합니다.

```cpp
sport_client_.StopMove(req_);
```

---

## 1.2 주행 순서 설계

코드를 작성하기 전에 이번 Waypoint 경로를 표로 정리합니다.

| 구간    | 동작  | API 값                  |    시간 |
| ----- | --- | ---------------------- | ----: |
| 출발    | 기립  | `StandUp()`            | 3초 대기 |
| WP1   | 전진  | `Move(0.2, 0.0, 0.0)`  |  1.0초 |
| 방향 변경 | 좌회전 | `Move(0.0, 0.0, 0.5)`  |  1.5초 |
| WP2   | 전진  | `Move(0.2, 0.0, 0.0)`  |  1.0초 |
| 방향 변경 | 우회전 | `Move(0.0, 0.0, -0.5)` |  1.5초 |
| WP3   | 전진  | `Move(0.2, 0.0, 0.0)`  |  1.0초 |
| 종료    | 정지  | `StopMove()`           |     - |

> 위 속도와 시간은 실습용 값입니다. 실제 이동 거리와 회전량은 바닥 상태와 로봇의 움직임에 따라 차이가 발생할 수 있습니다.

---

## 1.3 C++ 파일 생성

`my_go2_nav` 패키지의 `src` 폴더로 이동합니다.

```bash
cd ~/Desktop/unitree_ros2/src/my_go2_nav/src
```

새 C++ 파일을 생성합니다.

```bash
gedit student_waypoint_sport.cpp
```

아래 코드를 작성하고 저장합니다.

```cpp
#include <chrono>
#include <iostream>
#include <memory>
#include <thread>

#include <rclcpp/rclcpp.hpp>
#include "common/ros2_sport_client.h"


class StudentWaypointSport : public rclcpp::Node
{
public:

  StudentWaypointSport()
  : Node("student_waypoint_sport"),
    sport_client_(this)
  {
    RCLCPP_INFO(
      this->get_logger(),
      "Sport API Waypoint node started"
    );
  }


  // ========================================================
  // 일정 시간 동안 Go2를 이동시키는 함수
  // ========================================================
  //
  // vx       : 전후 이동 속도
  // vy       : 좌우 이동 속도
  // vyaw     : 회전 각속도
  // duration : 명령을 유지할 시간
  //
  void move_for(
    float vx,
    float vy,
    float vyaw,
    double duration_sec)
  {
    RCLCPP_INFO(
      this->get_logger(),
      "Move: vx=%.2f, vy=%.2f, vyaw=%.2f, time=%.1f",
      vx,
      vy,
      vyaw,
      duration_sec
    );

    // 이동 시작 시간 저장
    auto start =
      std::chrono::steady_clock::now();


    // 지정된 시간이 끝날 때까지 반복
    while (rclcpp::ok())
    {
      auto now =
        std::chrono::steady_clock::now();

      double elapsed =
        std::chrono::duration<double>(
          now - start
        ).count();


      // 목표 시간이 지나면 반복 종료
      if (elapsed >= duration_sec)
      {
        break;
      }


      // Go2 Sport API Move 명령 직접 호출
      sport_client_.Move(
        req_,
        vx,
        vy,
        vyaw
      );


      // ROS2 callback 처리
      rclcpp::spin_some(
        shared_from_this()
      );


      // 0.1초 대기
      // 약 10 Hz로 Move 명령 반복 전송
      std::this_thread::sleep_for(
        std::chrono::milliseconds(100)
      );
    }


    // 하나의 이동 구간이 끝나면 명시적으로 정지
    sport_client_.StopMove(req_);


    // 다음 동작 전에 잠시 대기
    std::this_thread::sleep_for(
      std::chrono::seconds(1)
    );
  }


  // ========================================================
  // 전체 Waypoint 주행
  // ========================================================

  void run_waypoint()
  {
    RCLCPP_INFO(
      this->get_logger(),
      "Waypoint sequence start"
    );


    // ------------------------------------------------------
    // 1. 기립
    // ------------------------------------------------------

    RCLCPP_INFO(
      this->get_logger(),
      "Stand Up"
    );

    sport_client_.StandUp(req_);


    // 실제 기립 동작이 완료될 시간을 확보
    std::this_thread::sleep_for(
      std::chrono::seconds(3)
    );


    // ------------------------------------------------------
    // 2. WP1까지 전진
    // ------------------------------------------------------

    RCLCPP_INFO(
      this->get_logger(),
      "WP1 : Forward"
    );

    move_for(
      0.2f,
      0.0f,
      0.0f,
      1.0
    );


    // ------------------------------------------------------
    // 3. 왼쪽 회전
    // ------------------------------------------------------

    RCLCPP_INFO(
      this->get_logger(),
      "Turn Left"
    );

    move_for(
      0.0f,
      0.0f,
      0.5f,
      1.5
    );


    // ------------------------------------------------------
    // 4. WP2까지 전진
    // ------------------------------------------------------

    RCLCPP_INFO(
      this->get_logger(),
      "WP2 : Forward"
    );

    move_for(
      0.2f,
      0.0f,
      0.0f,
      1.0
    );


    // ------------------------------------------------------
    // 5. 오른쪽 회전
    // ------------------------------------------------------

    RCLCPP_INFO(
      this->get_logger(),
      "Turn Right"
    );

    move_for(
      0.0f,
      0.0f,
      -0.5f,
      1.5
    );


    // ------------------------------------------------------
    // 6. WP3까지 전진
    // ------------------------------------------------------

    RCLCPP_INFO(
      this->get_logger(),
      "WP3 : Forward"
    );

    move_for(
      0.2f,
      0.0f,
      0.0f,
      1.0
    );


    // ------------------------------------------------------
    // 7. 최종 정지
    // ------------------------------------------------------

    sport_client_.StopMove(req_);

    RCLCPP_INFO(
      this->get_logger(),
      "Waypoint finished"
    );
  }


  // 프로그램 종료 전 사용할 정지 함수
  void stop()
  {
    sport_client_.StopMove(req_);
  }


private:

  // Go2 Sport API 명령을 사용하기 위한 Client
  SportClient sport_client_;

  // Unitree API Request 메시지
  unitree_api::msg::Request req_;
};


int main(int argc, char **argv)
{
  // ROS2 초기화
  rclcpp::init(argc, argv);


  // ROS2 노드 생성
  auto node =
    std::make_shared<StudentWaypointSport>();


  // DDS discovery를 위한 대기 시간
  std::this_thread::sleep_for(
    std::chrono::seconds(1)
  );


  // 사용자가 확인하기 전에는 로봇을 움직이지 않음
  std::cout << "\n";
  std::cout << "================================\n";
  std::cout << " Sport API Waypoint Test\n";
  std::cout << "================================\n";
  std::cout
    << "주변 공간과 비상정지 리모컨을 확인하세요.\n";
  std::cout
    << "Enter를 누르면 시작합니다.\n";


  std::cin.get();


  // Waypoint 주행 시작
  node->run_waypoint();


  // 프로그램 종료 전 다시 한 번 정지
  node->stop();


  rclcpp::shutdown();

  return 0;
}
```

---

## 1.4 코드 구조 확인

전체 코드를 한 번에 외울 필요는 없습니다.

다음 흐름만 먼저 확인합니다.

```text
StudentWaypointSport 노드 생성
        ↓
SportClient 준비
        ↓
Enter 입력 대기
        ↓
StandUp()
        ↓
move_for()
        ↓
Move() 반복 전송
        ↓
시간 종료
        ↓
StopMove()
        ↓
다음 구간
        ↓
최종 StopMove()
```

특히 이번 코드의 핵심은 직접 만든 다음 함수입니다.

```cpp
void move_for(
  float vx,
  float vy,
  float vyaw,
  double duration_sec)
```

이 함수 하나를 이용하여 전진도 만들고:

```cpp
move_for(
  0.2f,
  0.0f,
  0.0f,
  1.0
);
```

왼쪽 회전도 만들고:

```cpp
move_for(
  0.0f,
  0.0f,
  0.5f,
  1.5
);
```

오른쪽 회전도 만들 수 있습니다.

```cpp
move_for(
  0.0f,
  0.0f,
  -0.5f,
  1.5
);
```

즉 **같은 이동 함수를 값만 바꾸어 여러 동작에 재사용**하고 있습니다.

---

## 1.5 CMakeLists.txt에 실행 파일 등록

C++ 파일을 만들었다고 바로 `ros2 run`으로 실행할 수 있는 것은 아닙니다.

CMake에게 다음 내용을 알려주어야 합니다.

```text
어떤 C++ 파일을 컴파일할지
어떤 라이브러리를 사용할지
ROS2 실행 파일로 어디에 설치할지
```

다음 파일을 엽니다.

```bash
gedit ~/Desktop/unitree_ros2/src/my_go2_nav/CMakeLists.txt
```

`ament_package()`보다 위에 다음 내용을 추가합니다.

```cmake
# ============================================================
# 학생용 Sport API Waypoint
# ============================================================

# student_waypoint_sport.cpp를
# student_waypoint_sport라는 실행 파일로 생성
#
# ros2_sport_client.cpp도 함께 빌드하여
# SportClient 기능을 사용할 수 있게 함
add_executable(student_waypoint_sport
  src/student_waypoint_sport.cpp
  src/ros2_sport_client.cpp
)


# SportClient 관련 header를 찾을 위치
target_include_directories(student_waypoint_sport PUBLIC
  $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
  $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include/common>
  src
)


# 이 실행 파일에서 사용하는 ROS2 / Unitree 패키지
ament_target_dependencies(student_waypoint_sport
  rclcpp
  unitree_api
  unitree_go
)


# ros2 run에서 실행 파일을 찾을 수 있도록 설치
install(TARGETS
  student_waypoint_sport
  DESTINATION lib/${PROJECT_NAME}
)
```

### 우리가 CMakeLists.txt를 수정한 이유

이번에 새로 만든 파일은:

```text
student_waypoint_sport.cpp
```

입니다.

하지만 ROS2는 파일을 만들었다는 사실만으로 이 프로그램을 실행 파일로 인식하지 않습니다.

그래서:

```cmake
add_executable(...)
```

로 **무엇을 실행 파일로 만들지** 알려주고,

```cmake
ament_target_dependencies(...)
```

로 **어떤 ROS2/Unitree 기능을 사용하는지** 알려주고,

```cmake
install(...)
```

로 **`ros2 run`에서 찾을 수 있도록 등록**합니다.

---

## 1.6 빌드

작업공간으로 이동합니다.

```bash
cd ~/Desktop/unitree_ros2
```

환경을 적용합니다.

```bash
source /opt/ros/humble/setup.bash
source setup.sh
```

`my_go2_nav` 패키지를 빌드합니다.

```bash
colcon build --packages-select my_go2_nav
```

빌드가 완료되면:

```bash
source install/setup.bash
```

실행 파일이 등록되었는지 확인합니다.

```bash
ros2 pkg executables my_go2_nav | grep waypoint
```

다음 항목이 보이면 정상입니다.

```text
my_go2_nav student_waypoint_sport
```

---

## 1.7 실기체 실행 전 생각해 보기

실행하기 전에 코드만 보고 다음 질문에 답해 봅니다.

1. 로봇은 프로그램 실행 즉시 움직이는가?
2. `StandUp()` 뒤에 3초를 기다리는 이유는 무엇인가?
3. 전진할 때 `vx`, `vy`, `vyaw` 값은 각각 얼마인가?
4. 좌회전과 우회전의 코드 차이는 무엇인가?
5. `move_for()` 안에서 `Move()`를 한 번만 호출하지 않는 이유는 무엇인가?
6. 각 구간이 끝날 때 `StopMove()`를 호출하는 이유는 무엇인가?
7. 프로그램이 끝날 때 다시 `stop()`을 호출하는 이유는 무엇인가?

코드의 동작을 설명할 수 있다면 실제 로봇에서 실행합니다.

---

## 1.8 Sport API Waypoint 실행

새 터미널에서 다음을 실행합니다.

```bash
cd ~/Desktop/unitree_ros2

source /opt/ros/humble/setup.bash
source setup.sh
source install/setup.bash

ros2 run my_go2_nav student_waypoint_sport
```

이번 Waypoint는 Sport API를 직접 사용하므로 다음 브리지는 별도로 실행하지 않습니다.

```bash
ros2 run my_go2_nav go2_nav_bridge
```

프로그램을 실행하면 바로 이동하지 않고 다음 메시지에서 대기합니다.

```text
================================
 Sport API Waypoint Test
================================
주변 공간과 비상정지 리모컨을 확인하세요.
Enter를 누르면 시작합니다.
```

주변 안전과 리모컨을 확인한 뒤 `Enter`를 누릅니다.

예상되는 동작은 다음과 같습니다.

```text
StandUp
↓
전진
↓
StopMove
↓
왼쪽 회전
↓
StopMove
↓
전진
↓
StopMove
↓
오른쪽 회전
↓
StopMove
↓
전진
↓
StopMove
```

> 실제 로봇에서는 속도와 시간 조합이 원하는 거리와 정확히 일치하지 않을 수 있습니다. 시간 기반 주행에서는 바닥 마찰, 가감속, 회전 오차 등에 의해 실제 경로 오차가 발생할 수 있습니다.

---

## 1.9 실행 후 확인

주행이 끝난 뒤 다음을 생각해 봅니다.

* 예상한 거리만큼 전진했는가?
* 좌회전과 우회전의 회전량은 비슷했는가?
* 출발할 때와 마지막 방향은 같은가?
* 실제 경로가 예상과 다르다면 어느 구간에서 가장 큰 오차가 발생했는가?
* `Move()`의 속도를 바꿀 것인가, 실행 시간을 바꿀 것인가?

다음 실험에서는 **한 번에 하나의 값만 변경**하여 결과를 비교합니다.

예를 들어 다음 코드에서:

```cpp
move_for(
  0.2f,
  0.0f,
  0.0f,
  1.0
);
```

속도는 그대로 두고 시간만:

```text
1.0초 → 1.5초
```

로 변경하여 이동 거리의 차이를 관찰할 수 있습니다.
