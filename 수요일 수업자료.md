# 전체 목표

**명령을 보낸다 → 로봇이 움직인다 → 상태/센서로 그 결과를 확인한다**

---

```text
① 환경 확인
② teleop + 명령·상태 관찰
③ LiDAR + RViz
④ 직접 Twist 발행
⑤ 기록·도구 체험·정리
```

---

# 체크포인트 ① — 환경 확인

## 터미널 1: Gazebo 실행

```bash
source ~/go2_ws/install/setup.bash
ros2 launch go2_config gazebo.launch.py
```

## 확인 사항

```text
□ Gazebo가 정상 실행됐는가?
□ Go2 모델이 보이는가?
□ 로봇이 바닥에 정상적으로 서 있는가?
□ 월드와 장애물이 보이는가?
□ 반복되는 치명적 오류가 없는가?
□ RTF가 약 0.8 이상인가?
```

## ROS2 토픽 확인

새 터미널을 열고:

```bash
source ~/go2_ws/install/setup.bash
ros2 topic list -t
```

| 데이터 | 실제 토픽 이름 | 메시지 타입 |
| --- | --- | --- |
| 속도 명령 | `/cmd_vel` | `geometry_msgs/msg/Twist` |
| 로봇 이동 상태 | `/odom` | `nav_msgs/msg/Odometry` |
| LiDAR | `/velodyne_points` | `sensor_msgs/msg/PointCloud2` |
| 관절 상태 | `/joint_states` | `sensor_msgs/msg/JointState` |
| 발 접촉 상태 | `/foot_contacts` | `champ_msgs/msg/ContactsStamped` |
| 카메라 영상 | 없음 | 없음 |

## Gazebo 기본 조작

```text
□ Pause
□ Resume
□ Reset
```

로봇이 넘어지거나 이상해졌을 때 재실행만 찾지 말고, 일시정지와 Reset 기능도 사용합니다.

---

# 체크포인트 ② — Teleop과 상태 관찰

## 터미널 2: Teleop 실행

```bash
source ~/go2_ws/install/setup.bash
ros2 run teleop_twist_keyboard teleop_twist_keyboard
```

실제 키맵은 실행 화면을 기준으로 확인합니다.

일반적으로:

```text
i      전진
,      후진
j / l  좌우 회전
k      정지
```

게걸음과 속도 조절 키는 터미널의 키맵을 직접 보고 확인합니다.

## 학생 기록표

| 동작 | 키 | `/cmd_vel`에서 바뀐 필드 |
| --- | --- | --- |
| 전진 | `i` | `linear.x` |
| 후진 | `,` | `linear.x` |
| 왼쪽 게걸음 | `J` | `linear.y` |
| 오른쪽 게걸음 | `L` | `linear.y` |
| 좌회전 | `j` | `angular.z` |
| 우회전 | `l` | `angular.z` |
| 정지 | `k` | 모두 0 |

| 조절 | 키 | 의미 |
| --- | --- | --- |
| 전체 속도 올리기 | `q` | 선속도와 회전속도를 함께 증가 |
| 전체 속도 내리기 | `z` | 선속도와 회전속도를 함께 감소 |
| 선속도만 올리기 | `w` | 전진·후진·게걸음 속도 증가 |
| 선속도만 내리기 | `x` | 전진·후진·게걸음 속도 감소 |
| 회전속도만 올리기 | `e` | 좌·우 회전 속도 증가 |
| 회전속도만 내리기 | `c` | 좌·우 회전 속도 감소 |

## 터미널 3: 명령 메시지 관찰

```bash
source ~/go2_ws/install/setup.bash
ros2 topic echo /cmd_vel
```

Teleop에서 키를 누르며 값이 어떻게 바뀌는지 확인합니다.

### 전진

```text
linear:
  x: 양수
```

### 후진

```text
linear:
  x: 음수
```

### 게걸음

```text
linear:
  y: 양수 또는 음수
```

### 회전

```text
angular:
  z: 양수 또는 음수
```

## 정지 확인

```text
□ 정지 키를 눌렀을 때 멈추는가?
□ 키를 놓았을 때 자동으로 멈추는가?
□ 마지막 속도를 계속 유지하지는 않는가?
```

## 상태 확인: `/odom`

움직이기 전:

```bash
ros2 topic echo /odom --once
```

찾아볼 항목:

```text
header.frame_id
child_frame_id
pose
twist
```

그다음 로봇을 움직이고 다시 실행합니다.

```bash
ros2 topic echo /odom --once
```

전후 값을 비교합니다.

### 가능하면 추가 확인

```bash
ros2 topic hz /odom
ros2 topic info /odom
ros2 interface show nav_msgs/msg/Odometry
```

---

# 체크포인트 ③ — LiDAR와 RViz

기존 시뮬레이터를 종료합니다.

```text
Ctrl + C
```

LiDAR 시뮬레이션을 실행합니다.

```bash
source ~/go2_ws/install/setup.bash
ros2 launch go2_config gazebo_velodyne.launch.py rviz:=true
```

RViz에서 다음 순서로 LiDAR 데이터를 추가합니다.

```text
Add
→ By topic
→ /velodyne_points
→ PointCloud2
```

PointCloud2를 추가해야 LiDAR 점구름을 확인할 수 있습니다.

## LiDAR 토픽 조사

정확한 토픽 이름은 실제 실행 결과에서 확인합니다.

```bash
ros2 topic list -t
```

LiDAR 토픽을 찾은 뒤:

```bash
ros2 topic hz <LiDAR_토픽>
```

메타데이터를 확인합니다.

```bash
ros2 topic echo <LiDAR_토픽> --no-arr --once
```

확인할 것:

```text
발행 주기 Hz
header.stamp
header.frame_id
메시지 타입 sensor_msgs/msg/PointCloud2
```

## RViz 확인

```text
□ PointCloud2가 추가되어 있는가?
□ Fixed Frame이 올바른가?
□ 로봇 주변의 점들이 보이는가?
□ 장애물 윤곽이 점으로 나타나는가?
```

## 장애물 박스 추가

기본 월드에 관찰할 장애물이 부족할 때만 진행합니다.

파일 생성:

```bash
cat > /tmp/box.sdf <<'EOF'
<?xml version="1.0"?>
<sdf version="1.6">
  <model name="test_box">
    <static>true</static>
    <link name="link">
      <collision name="collision">
        <geometry>
          <box>
            <size>1 1 1</size>
          </box>
        </geometry>
      </collision>

      <visual name="visual">
        <geometry>
          <box>
            <size>1 1 1</size>
          </box>
        </geometry>
      </visual>
    </link>
  </model>
</sdf>
EOF
```

박스 생성:

```bash
source ~/go2_ws/install/setup.bash

ros2 run gazebo_ros spawn_entity.py \
  -entity test_box \
  -file /tmp/box.sdf \
  -x 2.0 \
  -y 0.0 \
  -z 0.5
```

각 값의 의미:

```text
x = 2.0
→ 로봇 앞쪽 약 2 m

y = 0.0
→ 좌우 중앙

z = 0.5
→ 높이 1 m 박스의 중심을 0.5 m에 두어 바닥에 놓음
```

## 관찰

```text
□ 박스 표면에 점이 나타나는가?
□ 박스의 모서리와 면 형태가 보이는가?
□ 박스 뒤쪽에 가려진 영역이 생기는가?
□ 로봇이 이동하면 점구름의 위치와 밀도가 어떻게 바뀌는가?
```

---

# 체크포인트 ④ — `/cmd_vel` 직접 발행

이 실습을 할 때는 **Teleop 노드를 먼저 종료하는 것을 권장합니다.**

다른 노드가 동시에 `/cmd_vel`을 발행하면 서로 다른 명령이 겹칠 수 있습니다.

## 직진

```bash
ros2 topic pub --rate 10 /cmd_vel geometry_msgs/msg/Twist \
"{linear: {x: 0.3, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

의미:

```text
--rate 10
→ 초당 10번 같은 명령을 발행

linear.x = 0.3
→ 로봇 몸통 기준 앞쪽으로 0.3 m/s
```

## 정지

먼저 지속 발행을 종료합니다.

```text
Ctrl + C
```

그다음 명시적으로 0 속도를 발행합니다.

```bash
ros2 topic pub --once /cmd_vel geometry_msgs/msg/Twist \
"{linear: {x: 0.0, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

## 제자리 회전

```bash
ros2 topic pub --rate 10 /cmd_vel geometry_msgs/msg/Twist \
"{linear: {x: 0.0, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.5}}"
```

## 원 궤적

```bash
ros2 topic pub --rate 10 /cmd_vel geometry_msgs/msg/Twist \
"{linear: {x: 0.3, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.3}}"
```

## 명령과 상태 동시에 비교

다른 터미널에서:

```bash
ros2 topic echo /odom
```

`/cmd_vel`로 보낸 명령과 `/odom`에서 관찰되는 이동 상태를 함께 비교합니다.

---

# 사각형 경로 주행 — Python

Teleop 노드는 먼저 종료합니다.

```text
Ctrl + C
```

작업 폴더로 이동합니다.

```bash
mkdir -p ~/robot_study/go2_week
cd ~/robot_study/go2_week
```

Python 파일을 엽니다.

```bash
gedit square_path.py
```

아래 코드를 붙여 넣고 저장합니다.

```python
#!/usr/bin/env python3

import subprocess
import time


LINEAR_SPEED = 0.3
ANGULAR_SPEED = 0.5

FORWARD_TIME = 3.33
TURN_TIME = 3.14

CMD_VEL_TOPIC = "/cmd_vel"
TWIST_TYPE = "geometry_msgs/msg/Twist"


def publish_for_duration(message: str, duration: float) -> None:
    """지정한 Twist 메시지를 10 Hz로 일정 시간 동안 발행한다."""

    command = [
        "ros2",
        "topic",
        "pub",
        "--rate",
        "10",
        CMD_VEL_TOPIC,
        TWIST_TYPE,
        message,
    ]

    process = subprocess.Popen(command)

    try:
        time.sleep(duration)

    finally:
        process.terminate()

        try:
            process.wait(timeout=2)

        except subprocess.TimeoutExpired:
            process.kill()
            process.wait()


def stop_robot() -> None:
    """0 속도 명령을 한 번 발행하여 명시적으로 정지한다."""

    stop_message = (
        "{linear: {x: 0.0, y: 0.0, z: 0.0}, "
        "angular: {x: 0.0, y: 0.0, z: 0.0}}"
    )

    subprocess.run(
        [
            "ros2",
            "topic",
            "pub",
            "--once",
            CMD_VEL_TOPIC,
            TWIST_TYPE,
            stop_message,
        ],
        check=True,
    )


def move_forward() -> None:

    forward_message = (
        f"{{linear: {{x: {LINEAR_SPEED}, y: 0.0, z: 0.0}}, "
        "angular: {x: 0.0, y: 0.0, z: 0.0}}"
    )

    publish_for_duration(
        forward_message,
        FORWARD_TIME
    )

    stop_robot()

    time.sleep(1.0)


def turn_left() -> None:

    turn_message = (
        "{linear: {x: 0.0, y: 0.0, z: 0.0}, "
        f"angular: {{x: 0.0, y: 0.0, z: {ANGULAR_SPEED}}}}}"
    )

    publish_for_duration(
        turn_message,
        TURN_TIME
    )

    stop_robot()

    time.sleep(1.0)


def main() -> None:

    try:

        for side in range(1, 5):

            print(
                f"{side}번째 변 직진"
            )

            move_forward()

            print(
                f"{side}번째 90도 좌회전"
            )

            turn_left()

        print(
            "사각형 경로 주행 완료"
        )

    except KeyboardInterrupt:

        print(
            "\n사용자 중단"
        )

    finally:

        stop_robot()

        print(
            "로봇 정지 명령 전송 완료"
        )


if __name__ == "__main__":
    main()
```

실행 권한을 부여합니다.

```bash
chmod +x square_path.py
```

작업공간 환경을 적용합니다.

```bash
source ~/go2_ws/install/setup.bash
```

실행:

```bash
python3 square_path.py
```

중간에 멈추려면:

```text
Ctrl + C
```

## 참고

```text
직진:

1 m ÷ 0.3 m/s
≈ 3.33초


회전:

90° ≈ 1.57 rad

1.57 rad ÷ 0.5 rad/s
≈ 3.14초
```

실제 시뮬레이션에서는 가감속, 마찰, 명령 처리 주기 등의 영향으로 계산값과 차이가 날 수 있습니다.

---

# 체크포인트 ⑤ — 조사 도구와 기록

## 1. rosbag 녹화

작업 폴더 생성:

```bash
mkdir -p ~/robot_study/go2_week
cd ~/robot_study/go2_week
```

녹화:

```bash
ros2 bag record -o sim_scan /velodyne_points /odom
```

약 30초 정도 기록한 뒤:

```text
Ctrl + C
```

정보 확인:

```bash
ros2 bag info sim_scan
```

## 2. rqt_graph

```bash
ros2 run rqt_graph rqt_graph
```

찾아볼 경로:

```text
teleop 또는 topic pub
→ /cmd_vel
→ Go2 제어 노드
```

그리고:

```text
Gazebo LiDAR 플러그인
→ PointCloud2 토픽
→ RViz
```

## 3. 대역폭 확인

시간이 남을 때:

```bash
ros2 topic bw <LiDAR_토픽>
```

비교할 수 있다면:

```bash
ros2 topic bw /odom
ros2 topic bw /cmd_vel
ros2 topic bw <LiDAR_토픽>
```

---

# 필수 기록

체크포인트별로 다음 내용을 기록합니다.

```text
완료 시각:
성공 여부:
실제 토픽 이름:
메시지 타입:
측정 주기:
막힌 점:
해결 방법:
```

반드시 남길 화면:

```text
1. Gazebo + RTF 화면
2. /cmd_vel echo에서 값이 바뀌는 화면
3. /odom 전후 비교
4. RViz PointCloud 화면
5. 원 궤적 또는 곡선 이동 화면
6. ros2 bag info 결과
7. rqt_graph
```

교재에서는 RViz 화면과 원 궤적 화면을 주요 산출물로 다루며, 조사 실습에서는 bag과 rqt_graph도 함께 활용합니다.

---

# 시간이 남으면 학생 도전 과제

## 도전 1 — 월드 평면도

Gazebo를 위에서 내려다보고 다음 내용을 표시합니다.

```text
Go2 시작 위치
장애물 위치
이동 가능한 통로
Waypoint 후보 2~3개
```

손으로 간단히 그려도 됩니다.

---

## 도전 2 — 월드 파일 찾기

```bash
find ~/go2_ws/src -type f \( -name "*.world" -o -name "*.sdf" \)
```

실제 저장소 경로가 `~/robot_ws`라면 해당 경로로 변경합니다.

`pose`가 포함된 부분을 찾으려면:

```bash
grep -R -n "<pose>" ~/go2_ws/src | head -30
```

또는 찾은 월드 파일을 직접 엽니다.

```bash
gedit <찾은_월드파일>
```

확인할 내용:

```xml
<pose>x y z roll pitch yaw</pose>
```

---

## 도전 3 — Fixed Frame 두 시점 비교

RViz의 Fixed Frame을 다음 두 값으로 바꾸면서 로봇을 회전합니다.

```text
LiDAR frame
odom
```

예상 결과:

```text
LiDAR frame
→ 로봇은 고정되고 주변 세계가 움직여 보임

odom
→ 세계는 고정되고 로봇이 움직여 보임
```

단, `odom ↔ LiDAR frame` 사이의 TF가 제공되는 경우에만 정상적으로 표시됩니다.

---

## 도전 4 — bag 재생

시뮬레이터를 종료한 뒤:

```bash
ros2 bag play ~/robot_study/go2_week/sim_scan
```

RViz에서 PointCloud가 다시 표시되는지 확인합니다.

상황에 따라 `/clock` 또는 `use_sim_time` 설정 때문에 바로 보이지 않을 수도 있습니다.

---

## 도전 5 — 사각형 경로

다음 구조로 설계합니다.

```text
전진
→ 정지
→ 90° 회전
→ 정지
```

이 동작을 네 번 반복합니다.

기본 계산:

```text
1 m를 0.3 m/s로 이동
→ 약 3.33초

90° = 약 1.57 rad

0.5 rad/s로 회전
→ 약 3.14초
```

시뮬레이션에서 출발점으로 정확히 돌아오지 않는다면 그 이유를 기록합니다.

```text
가감속
마찰
회전 시간 근사
명령 발행 주기
오도메트리 오차
```

---

# 수요일 시간 부족 시 우선순위

## 반드시 한다

```text
1. Gazebo 실행과 RTF 확인
2. Teleop과 정지 확인
3. /cmd_vel echo
4. /odom 전후 비교
5. RViz PointCloud 확인
6. 직접 직진·회전·원 명령
7. 명시적 정지
8. 학습일지 작성
```

## 가능하면 한다

```text
9. LiDAR Hz / frame_id 확인
10. 박스 Spawn
11. rosbag record / info
12. rqt_graph
```

## 시간이 남으면 한다

```text
13. bw 비교
14. bag play
15. Fixed Frame 비교
16. 월드 파일 확인
17. 사각형 경로
```
