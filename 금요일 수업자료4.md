# 2. LiDAR 장애물 감지 및 자동 정지

이번 실습에서는 Hesai LiDAR의 `/lidar_points`를 구독하여 Go2 전방의 장애물을 감지하고, 일정 거리 안으로 장애물이 들어오면 자동으로 정지하도록 만들어 봅니다.

전체 동작 구조는 다음과 같습니다.

```text
/lidar_points
      ↓
PointCloud2 수신
      ↓
Go2 전방 영역의 점만 선택
      ↓
가장 가까운 점까지의 거리 계산
      ↓
장애물 거리 판단
      ↓
0.6 m 미만 → 정지
0.8 m 초과 → 다시 출발
      ↓
/cmd_vel 발행
      ↓
go2_nav_bridge
      ↓
Go2
```

Waypoint 실습에서는 Sport API를 직접 사용했지만, 이번 LiDAR Stop에서는 센서 판단 결과에 따라 `/cmd_vel`을 지속적으로 발행하는 구조를 사용합니다.

따라서 이번 실습에서는 `go2_nav_bridge`가 필요합니다.

---

## 2.1 먼저 생각해 보기 — 장애물을 보고 멈추려면 무엇이 필요할까?

완성된 코드를 작성하기 전에 로봇이 장애물을 보고 멈추기 위해 필요한 과정을 먼저 나누어 봅니다.

```text
1. LiDAR 데이터를 받는다.

2. LiDAR의 모든 점 중
   로봇 앞쪽에 있는 점만 선택한다.

3. 선택한 점 중
   가장 가까운 점까지의 거리를 구한다.

4. 거리가 너무 가까우면 정지한다.

5. 장애물이 다시 멀어지면 출발한다.

6. 판단 결과에 따라
   이동 명령을 계속 Go2에 보낸다.
```

각 단계를 코드로 어떻게 만들 수 있을지 하나씩 확인해 봅니다.

---

## 2.2 생각 ① LiDAR 데이터를 어떻게 받을까?

Hesai LiDAR는 다음 토픽으로 PointCloud 데이터를 발행합니다.

```text
/lidar_points
```

메시지 타입은:

```text
sensor_msgs/msg/PointCloud2
```

입니다.

따라서 Python 코드에서는 다음 메시지 타입을 불러옵니다.

```python
from sensor_msgs.msg import PointCloud2
```

그리고 Subscriber를 만듭니다.

```python
self.subscription = self.create_subscription(
    PointCloud2,
    '/lidar_points',
    self.lidar_callback,
    qos_profile_sensor_data
)
```

새로운 LiDAR 데이터가 들어올 때마다:

```python
self.lidar_callback
```

함수가 실행됩니다.

즉:

```text
/lidar_points 데이터 도착
        ↓
lidar_callback() 실행
```

이라는 구조입니다.

---

## 2.3 생각 ② PointCloud2 안의 x, y, z는 어떻게 읽을까?

PointCloud2에는 매우 많은 점이 포함되어 있습니다.

각 점에는 다음과 같은 정보가 들어 있습니다.

```text
x
y
z
intensity
ring
timestamp
```

하지만 `PointCloud2.data`는 이 값들을 사람이 바로 읽을 수 있는 형태가 아니라 **바이너리 데이터**로 저장합니다.

따라서 Hesai PointCloud의 구조를 NumPy에 알려줍니다.

```python
point_dtype = np.dtype({
    'names': [
        'x',
        'y',
        'z',
        'intensity',
        'ring',
        'timestamp'
    ],

    'formats': [
        '<f4',
        '<f4',
        '<f4',
        '<f4',
        '<u2',
        '<f8'
    ],

    'offsets': [
        0,
        4,
        8,
        12,
        16,
        18
    ],

    'itemsize': msg.point_step
})
```

그다음 PointCloud2의 byte 데이터를 NumPy 배열로 변환합니다.

```python
points = np.frombuffer(
    msg.data,
    dtype=point_dtype,
    count=msg.width * msg.height
)
```

이제 다음과 같이 좌표값을 가져올 수 있습니다.

```python
x = points['x']
y = points['y']
z = points['z']
```

---

## 2.4 생각 ③ 모든 LiDAR 점을 장애물로 봐야 할까?

아닙니다.

Go2가 앞으로 이동하는 상황에서 뒤쪽이나 멀리 떨어진 점까지 모두 장애물로 판단할 필요는 없습니다.

따라서 **로봇 전방의 일정 영역만 검사**합니다.

이번 실습에서는 다음 범위를 사용합니다.

```text
X
→ 0.2 m ~ 3.0 m

Y
→ -0.4 m ~ +0.4 m

Z
→ -0.5 m ~ +0.5 m
```

코드에서는 다음과 같이 표현합니다.

```python
mask = (
    np.isfinite(x) &
    np.isfinite(y) &
    np.isfinite(z) &

    (x > 0.2) &
    (x < 3.0) &

    (np.abs(y) < 0.4) &

    (z > -0.5) &
    (z < 0.5)
)
```

이 조건은 Go2 앞쪽에 **가상의 장애물 검사 영역**을 만드는 것과 같습니다.

```text
          전방 +X

      ┌───────────────┐
      │               │
      │  검사 영역    │
      │               │
      └───────────────┘
            [Go2]

      좌우 약 ±0.4 m
```

`mask`의 조건을 만족하는 점만 이후 장애물 판단에 사용합니다.

---

## 2.5 생각 ④ 가장 가까운 장애물은 어떻게 찾을까?

전방 영역 안에 있는 점의 `x`, `y` 좌표를 사용합니다.

```python
front_x = x[mask]
front_y = y[mask]
```

각 점까지의 평면 거리는 다음 관계를 이용할 수 있습니다.

```text
거리² = x² + y²
```

코드에서는:

```python
distance_sq = (
    front_x * front_x
    + front_y * front_y
)
```

로 계산합니다.

그중 가장 작은 값을 찾습니다.

```python
min_index = np.argmin(
    distance_sq
)
```

마지막으로 제곱근을 계산하면 실제 거리를 얻을 수 있습니다.

```python
self.min_distance = math.sqrt(
    float(
        distance_sq[min_index]
    )
)
```

따라서 `self.min_distance`에는:

```text
Go2 전방 검사 영역에 있는
LiDAR 점 중
가장 가까운 점까지의 거리
```

가 저장됩니다.

---

## 2.6 생각 ⑤ 몇 m에서 멈추게 할까?

이번 실습에서는 다음 기준을 사용합니다.

```python
self.stop_distance = 0.6
self.restart_distance = 0.8
```

즉:

```text
장애물 거리 < 0.6 m
→ 정지

장애물 거리 > 0.8 m
→ 다시 출발
```

입니다.

그렇다면 왜 둘 다 `0.6 m`로 하지 않을까요?

만약 정지와 출발 기준을 모두 0.6 m로 설정하면 센서 값이 다음처럼 조금씩 흔들릴 수 있습니다.

```text
0.59 m → STOP
0.61 m → GO
0.58 m → STOP
0.62 m → GO
```

그러면 정지와 출발이 빠르게 반복될 수 있습니다.

이를 줄이기 위해:

```text
0.6 m에서 정지
        ↓
0.8 m까지 충분히 멀어진 후 재출발
```

하도록 두 기준 사이에 간격을 둡니다.

이러한 방식을 **히스테리시스(Hysteresis)**라고 합니다.

---

## 2.7 생각 ⑥ LiDAR가 들어올 때만 속도를 보내도 될까?

이번 코드에서 중요한 부분입니다.

처음에는 다음처럼 생각할 수 있습니다.

```text
LiDAR 데이터 수신
→ 거리 계산
→ /cmd_vel 한 번 발행
```

하지만 로봇의 이동 명령은 일정 주기로 계속 전달하는 것이 안정적입니다.

또한 순간적으로 전방에 유효한 LiDAR 점이 없더라도 이동 명령 자체가 끊기지 않도록:

```text
LiDAR callback
→ 장애물 여부 판단

Timer
→ 이동 또는 정지 명령 지속 발행
```

으로 역할을 나눕니다.

이번 코드에서는 다음 Timer를 사용합니다.

```python
self.cmd_timer = self.create_timer(
    0.1,
    self.publish_cmd
)
```

`0.1초`마다 실행되므로 약:

```text
10 Hz
```

로 `/cmd_vel`이 발행됩니다.

---

# 3. LiDAR Stop Python 파일 생성

Python 코드 폴더로 이동합니다.

```bash
cd ~/Desktop/unitree_ros2/src/go2_practice/go2_practice
```

새 파일을 생성합니다.

```bash
gedit student_lidar_stop.py
```

아래 코드를 작성하고 저장합니다.

```python
import math

import numpy as np

import rclpy
from rclpy.node import Node
from rclpy.qos import qos_profile_sensor_data

from geometry_msgs.msg import Twist
from sensor_msgs.msg import PointCloud2


class StudentLidarStop(Node):

    def __init__(self):

        # ROS2 노드 이름 설정
        super().__init__('student_lidar_stop')


        # ==================================================
        # /cmd_vel Publisher
        # ==================================================
        #
        # 장애물 판단 결과에 따라
        # 전진 또는 정지 속도 명령을 발행한다.
        #
        self.publisher_ = self.create_publisher(
            Twist,
            '/cmd_vel',
            10
        )


        # ==================================================
        # Hesai LiDAR Subscriber
        # ==================================================
        #
        # /lidar_points의 PointCloud2 데이터를 구독한다.
        #
        # 새로운 데이터가 들어오면
        # lidar_callback() 함수가 실행된다.
        #
        self.subscription = self.create_subscription(
            PointCloud2,
            '/lidar_points',
            self.lidar_callback,
            qos_profile_sensor_data
        )


        # ==================================================
        # 장애물 판단 상태
        # ==================================================

        # False
        # → 주행 가능

        # True
        # → 장애물이 가까워 정지
        self.stopped = False


        # 장애물이 이 거리보다 가까우면 정지
        self.stop_distance = 0.6

        # 장애물이 이 거리보다 멀어져야 다시 출발
        self.restart_distance = 0.8


        # 가장 최근에 측정한 최소 거리
        self.min_distance = None


        # ==================================================
        # 속도 명령 Timer
        # ==================================================
        #
        # 0.1초마다 publish_cmd()를 실행한다.
        #
        # 즉 약 10 Hz로 /cmd_vel을 계속 발행한다.
        #
        self.cmd_timer = self.create_timer(
            0.1,
            self.publish_cmd
        )


        self.get_logger().info(
            'Student LiDAR stop node started'
        )


    # ======================================================
    # LiDAR 데이터 처리
    # ======================================================

    def lidar_callback(self, msg):

        # --------------------------------------------------
        # Hesai PointCloud2 데이터 구조
        # --------------------------------------------------
        #
        # PointCloud2의 data는 바이너리 형태이므로
        # 각 값의 위치와 자료형을 정의한다.
        #
        point_dtype = np.dtype({

            'names': [
                'x',
                'y',
                'z',
                'intensity',
                'ring',
                'timestamp'
            ],

            'formats': [
                '<f4',
                '<f4',
                '<f4',
                '<f4',
                '<u2',
                '<f8'
            ],

            'offsets': [
                0,
                4,
                8,
                12,
                16,
                18
            ],

            'itemsize': msg.point_step
        })


        # --------------------------------------------------
        # PointCloud2 byte 데이터 → NumPy 배열
        # --------------------------------------------------

        points = np.frombuffer(
            msg.data,
            dtype=point_dtype,
            count=msg.width * msg.height
        )


        # PointCloud가 비어 있다면 처리하지 않는다.
        if points.size == 0:
            return


        # 각 점의 x, y, z 좌표 가져오기
        x = points['x']
        y = points['y']
        z = points['z']


        # ==================================================
        # Go2 전방 영역(ROI) 선택
        # ==================================================
        #
        # 모든 LiDAR 점을 사용하는 것이 아니라
        # Go2가 앞으로 이동할 때 충돌할 가능성이 있는
        # 전방 영역의 점만 선택한다.
        #
        mask = (
            np.isfinite(x) &
            np.isfinite(y) &
            np.isfinite(z) &

            # 로봇 바로 주변의 점 제외
            (x > 0.2) &

            # 전방 최대 3 m까지 검사
            (x < 3.0) &

            # 좌우 약 ±0.4 m
            (np.abs(y) < 0.4) &

            # 지나치게 높거나 낮은 점 제외
            (z > -0.5) &
            (z < 0.5)
        )


        # ==================================================
        # 전방 영역에 유효한 점이 없는 경우
        # ==================================================

        if not np.any(mask):

            # 현재 검출된 최소 거리 없음
            self.min_distance = None


            # 이전에 장애물 때문에 정지 중이었다면
            # 장애물이 사라진 것으로 보고 다시 주행 가능 상태
            if self.stopped:

                self.stopped = False

                self.get_logger().info(
                    'GO resumed | no obstacle'
                )

            return


        # ==================================================
        # 전방 영역의 점만 추출
        # ==================================================

        front_x = x[mask]
        front_y = y[mask]


        # ==================================================
        # 각 점까지의 평면 거리 계산
        #
        # distance² = x² + y²
        # ==================================================

        distance_sq = (
            front_x * front_x
            + front_y * front_y
        )


        # 가장 가까운 점의 index
        min_index = np.argmin(
            distance_sq
        )


        # 실제 최소 거리 계산
        self.min_distance = math.sqrt(
            float(
                distance_sq[min_index]
            )
        )


        # ==================================================
        # 장애물 판단 — 히스테리시스
        # ==================================================

        # --------------------------------------------------
        # 주행 중이며 장애물이 0.6 m보다 가까우면 정지
        # --------------------------------------------------

        if (
            not self.stopped
            and self.min_distance < self.stop_distance
        ):

            self.stopped = True

            self.get_logger().info(
                f'STOP triggered | '
                f'{self.min_distance:.2f} m'
            )


        # --------------------------------------------------
        # 정지 중이며 장애물이 0.8 m보다 멀어지면 재출발
        # --------------------------------------------------

        elif (
            self.stopped
            and self.min_distance > self.restart_distance
        ):

            self.stopped = False

            self.get_logger().info(
                f'GO resumed | '
                f'{self.min_distance:.2f} m'
            )


    # ======================================================
    # 속도 명령 발행
    # ======================================================

    def publish_cmd(self):

        # Twist 메시지 생성
        cmd = Twist()


        # 장애물이 가까우면 정지
        if self.stopped:

            cmd.linear.x = 0.0


        # 장애물이 없으면 전진
        else:

            # 전진 속도 0.2 m/s
            cmd.linear.x = 0.2


        # 좌우 이동 없음
        cmd.linear.y = 0.0

        # 회전 없음
        cmd.angular.z = 0.0


        # /cmd_vel 발행
        self.publisher_.publish(cmd)


    # ======================================================
    # 프로그램 종료 시 사용할 정지 함수
    # ======================================================

    def publish_stop(self):

        stop = Twist()

        stop.linear.x = 0.0
        stop.linear.y = 0.0
        stop.angular.z = 0.0

        self.publisher_.publish(stop)


# ==========================================================
# 프로그램 시작점
# ==========================================================

def main(args=None):

    # ROS2 초기화
    rclpy.init(args=args)


    # LiDAR Stop 노드 생성
    node = StudentLidarStop()


    try:

        # Subscriber callback과
        # Timer callback을 계속 처리
        rclpy.spin(node)


    except KeyboardInterrupt:

        pass


    finally:

        # 종료 전에 반드시 정지 명령 전송
        node.publish_stop()


        # 노드 제거
        node.destroy_node()


        # ROS2 종료
        if rclpy.ok():
            rclpy.shutdown()


if __name__ == '__main__':
    main()
```

---

## 3.1 코드 구조 확인

전체 코드는 다음 구조로 동작합니다.

```text
StudentLidarStop 노드 생성
        ↓
/lidar_points 구독
        ↓
lidar_callback()
        ↓
전방 영역의 점만 선택
        ↓
가장 가까운 거리 계산
        ↓
self.stopped 값 결정
        ↓
publish_cmd() Timer
        ↓
약 10 Hz로 /cmd_vel 발행
        ↓
go2_nav_bridge
        ↓
Go2
```

여기서 역할이 두 부분으로 나뉜다는 점이 중요합니다.

### 센서 판단

```python
def lidar_callback(self, msg):
```

에서는:

```text
LiDAR 수신
→ 전방 영역 선택
→ 거리 계산
→ STOP / GO 판단
```

을 수행합니다.

### 이동 명령

```python
def publish_cmd(self):
```

에서는:

```text
self.stopped 확인
→ 전진 또는 정지
→ /cmd_vel 발행
```

을 수행합니다.

즉:

```text
센서가 판단한다.
        +
Timer가 명령을 보낸다.
```

의 구조입니다.

---

# 4. LiDAR Stop 실행 파일 등록

Python 파일을 새로 만들었다고 바로 `ros2 run`으로 실행할 수 있는 것은 아닙니다.

Python 패키지에서는 `setup.py`의 `console_scripts`에 실행할 노드를 등록해야 합니다.

다음 파일을 엽니다.

```bash
gedit ~/Desktop/unitree_ros2/src/go2_practice/setup.py
```

기존 `console_scripts`에 다음 항목을 추가합니다.

```python
'student_lidar_stop = go2_practice.student_lidar_stop:main',
```

예를 들어:

```python
entry_points={
    'console_scripts': [
        'waypoint = go2_practice.waypoint:main',
        'lidar_distance = go2_practice.lidar_distance:main',
        'lidar_stop = go2_practice.lidar_stop:main',

        'student_lidar_stop = go2_practice.student_lidar_stop:main',
    ],
},
```

기존 실행 파일 등록 내용은 삭제하지 않습니다.

---

## 4.1 왜 `setup.py`를 수정할까?

이번에 만든 파일은:

```text
student_lidar_stop.py
```

입니다.

하지만 Python 파일을 만드는 것만으로는 ROS2가 다음 명령을 알 수 없습니다.

```bash
ros2 run go2_practice student_lidar_stop
```

그래서:

```python
'student_lidar_stop = go2_practice.student_lidar_stop:main',
```

을 등록하여:

```text
student_lidar_stop
        ↓
student_lidar_stop.py
        ↓
main()
```

을 연결합니다.

Waypoint의 C++ 실습에서 `CMakeLists.txt`를 수정했던 것과 비슷한 역할이지만, Python 패키지에서는 `setup.py`를 사용합니다.

---

# 5. LiDAR Stop 빌드

작업공간으로 이동합니다.

```bash
cd ~/Desktop/unitree_ros2
```

환경을 적용합니다.

```bash
source /opt/ros/humble/setup.bash
source setup.sh
```

`go2_practice` 패키지를 빌드합니다.

```bash
colcon build --symlink-install --packages-select go2_practice
```

빌드가 완료되면:

```bash
source install/setup.bash
```

실행 파일 등록 여부를 확인합니다.

```bash
ros2 pkg executables go2_practice | grep lidar
```

다음 항목이 보이면 정상입니다.

```text
go2_practice student_lidar_stop
```

---

# 6. 실행하기 전에 먼저 확인하기

실제 로봇을 움직이기 전에 코드만 보고 다음 질문에 답해 봅니다.

1. 이 코드는 어떤 토픽을 구독하는가?
2. `/lidar_points`의 메시지 타입은 무엇인가?
3. 왜 모든 LiDAR 점을 사용하지 않고 `mask`를 사용하는가?
4. `x > 0.2`, `x < 3.0`은 어떤 공간을 의미하는가?
5. `np.abs(y) < 0.4`는 왜 필요한가?
6. 가장 가까운 장애물의 거리는 어떤 값으로 계산하는가?
7. 왜 정지 거리는 `0.6 m`, 재출발 거리는 `0.8 m`로 다르게 설정했는가?
8. `lidar_callback()`과 `publish_cmd()`의 역할은 어떻게 다른가?
9. 왜 `/cmd_vel`을 한 번만 보내지 않고 0.1초마다 반복해서 보내는가?
10. 프로그램을 `Ctrl+C`로 종료하면 어떤 정지 코드가 실행되는가?

코드의 흐름을 설명할 수 있다면 실기체에서 실행합니다.

---

# 7. LiDAR Stop 실행 준비

이번 실습에서는 다음 프로그램들이 필요합니다.

```text
Hesai LiDAR Driver
        ↓
  /lidar_points
        ↓
student_lidar_stop
        ↓
    /cmd_vel
        ↓
go2_nav_bridge
        ↓
Unitree Sport API
        ↓
       Go2
```

Waypoint와 달리 이번 실습에서는 `/cmd_vel`을 사용하므로 `go2_nav_bridge`를 실행해야 합니다.

또한 `/cmd_vel`을 발행하는 여러 노드를 동시에 실행하면 서로 다른 이동 명령이 충돌할 수 있습니다.

따라서 다음 프로그램들은 모두 종료한 상태에서 진행합니다.

```text
teleop_twist_keyboard
다른 waypoint 노드
기존 lidar_stop
기타 /cmd_vel 발행 노드
```

---

## 7.1 터미널 1 — Hesai LiDAR Driver

새 터미널을 열고:

```bash
cd ~/hesai_ws

source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 launch hesai_ros_driver start.py
```

를 실행합니다.

이 터미널은 계속 실행한 상태로 둡니다.

---

## 7.2 LiDAR 데이터 먼저 확인

실제 주행 노드를 실행하기 전에 LiDAR가 정상적으로 들어오는지 확인합니다.

새 터미널:

```bash
source /opt/ros/humble/setup.bash
source ~/hesai_ws/install/setup.bash

ros2 topic hz /lidar_points
```

다음과 같이 주기가 계속 출력되면 정상입니다.

```text
average rate: ...
```

출력이 없다면 주행을 시작하지 않고 먼저 LiDAR Driver와 네트워크를 확인합니다.

---

## 7.3 터미널 2 — Go2 Bridge

새 터미널에서:

```bash
cd ~/Desktop/unitree_ros2

source /opt/ros/humble/setup.bash
source setup.sh
source install/setup.bash

ros2 run my_go2_nav go2_nav_bridge
```

를 실행합니다.

이 터미널도 계속 실행합니다.

`go2_nav_bridge`의 역할은:

```text
/cmd_vel
    ↓
Go2 Sport API 이동 명령
```

으로 변환하는 것입니다.

---

## 7.4 터미널 3 — LiDAR Stop

새 터미널에서:

```bash
cd ~/Desktop/unitree_ros2

source /opt/ros/humble/setup.bash
source setup.sh
source install/setup.bash

ros2 run go2_practice student_lidar_stop
```

를 실행합니다.

---

# 8. 실기체 동작 확인

기본 설정은 다음과 같습니다.

```text
전진 속도
→ 0.2 m/s

정지 조건
→ 장애물 거리 < 0.6 m

재출발 조건
→ 장애물 거리 > 0.8 m
```

예상되는 동작은 다음과 같습니다.

```text
장애물 없음
        ↓
0.2 m/s 전진
        ↓
장애물이 가까워짐
        ↓
최소 거리 감소
        ↓
0.6 m 미만
        ↓
self.stopped = True
        ↓
linear.x = 0.0
        ↓
Go2 정지
```

장애물을 다시 멀리 이동시키면:

```text
장애물 거리 > 0.8 m
        ↓
self.stopped = False
        ↓
linear.x = 0.2
        ↓
Go2 재출발
```

터미널에는 장애물 감지 시 다음과 같은 로그가 나타납니다.

```text
STOP triggered | 0.55 m
```

장애물을 멀리 이동시키면:

```text
GO resumed | 0.91 m
```

또는 전방 검사 영역에서 장애물이 완전히 사라진 경우:

```text
GO resumed | no obstacle
```

와 같이 나타날 수 있습니다.

---

# 9. 실행 후 생각해 보기

실습이 끝난 뒤 결과를 확인합니다.

* 실제로 약 `0.6 m` 부근에서 정지했는가?
* 장애물을 치웠을 때 다시 출발했는가?
* 장애물이 로봇 정면이 아니라 옆에 있을 때도 정지했는가?
* 그렇다면 `y` 검사 범위는 적절했는가?
* 바닥이나 로봇 자신의 일부가 장애물로 검출되지는 않았는가?
* `z` 범위를 바꾸면 어떤 점들이 새롭게 포함될까?
* 정지 거리를 `0.6 m → 0.8 m`로 늘리면 로봇의 움직임은 어떻게 달라질까?

특히 다음 부분을 수정하면 **전방 감지 영역**을 직접 바꿀 수 있습니다.

```python
(x > 0.2) &
(x < 3.0) &
(np.abs(y) < 0.4) &
(z > -0.5) &
(z < 0.5)
```

예를 들어:

```python
np.abs(y) < 0.6
```

으로 변경하면 좌우로 더 넓은 영역을 장애물 검사 범위에 포함하게 됩니다.

값을 변경할 때는 한 번에 하나의 조건만 수정하고 결과를 비교합니다.

---

# 10. 실습 종료

먼저 LiDAR Stop 노드를 종료합니다.

```text
Ctrl + C
```

프로그램의 `finally` 부분에서:

```python
node.publish_stop()
```

이 호출되어 `/cmd_vel`에 정지 명령을 전송합니다.

그다음 Go2가 실제로 멈췄는지 확인한 뒤:

```text
Go2 Bridge
Hesai LiDAR Driver
```

도 각각:

```text
Ctrl + C
```

로 종료합니다.

실제 Go2가 완전히 정지한 것을 확인한 뒤 실습을 마칩니다.

---

# 선택 실습 — 주행 데이터 rosbag 기록

LiDAR Stop 실습 동안 센서 데이터와 로봇의 이동 상태를 기록해 둘 수 있습니다.

별도의 터미널을 열고 환경을 적용합니다.

```bash
cd ~/Desktop/unitree_ros2

source /opt/ros/humble/setup.bash
source setup.sh
source install/setup.bash
```

LiDAR와 오도메트리를 함께 기록합니다.

```bash
ros2 bag record -o go2_lidar_stop /lidar_points /odom
```

이 상태에서 LiDAR Stop 실습을 수행합니다.

주행이 끝나면:

```text
Ctrl + C
```

로 rosbag 기록을 종료합니다.

저장 결과를 확인합니다.

```bash
ros2 bag info go2_lidar_stop
```

rosbag에는 실험 당시의 LiDAR와 오도메트리 데이터가 저장되므로 이후 실제 Go2가 없어도 당시 데이터를 다시 재생하여 분석할 수 있습니다.
