# 금요일 수업자료 1 — 연결/통신 문제 진단

## 1. 실습 목표

> 이번 실습에서는 명령어를 단순히 실행하는 데서 끝나지 않고, ROS2 노드와 토픽이 어떻게 연결되어 실제 Go2가 움직이는지 확인합니다.  
> 또한 연결 또는 통신 문제가 발생했을 때 어느 단계에서 문제가 생겼는지 진단하는 방법을 익힙니다.

문제가 발생했을 때는 아래 흐름을 기준으로 확인합니다.

```text
ping 실패
→ 네트워크 연결 또는 IP 설정 문제

ping 성공, 노드가 안 보임
→ DDS · RMW · 네트워크 인터페이스 · Domain ID 확인

토픽 이름은 보이지만 Publisher가 0
→ 드라이버 또는 발행 노드가 실행되지 않음

Publisher는 있지만 hz가 나오지 않음
→ 센서 · 드라이버 · QoS 문제 확인

명령 토픽 값은 정상
→ 구독 노드와 실제 로봇 연결 단계 확인
```

---

# 2. 연결/통신 문제 진단

Go2가 움직이지 않거나 토픽이 보이지 않을 때는 아래 순서대로 확인합니다.

## 2.1 네트워크 연결 확인

먼저 현재 PC의 네트워크 인터페이스와 IP 주소를 확인합니다.

```bash
ip addr
```

그다음 로봇 PC와 정상적으로 통신 가능한지 확인합니다.

예:

```bash
ping 192.168.123.161
```

정상일 경우 다음과 비슷한 응답이 반복해서 나타납니다.

```text
64 bytes from ...
```

실패할 경우 다음과 같은 메시지가 나타날 수 있습니다.

```text
Destination Host Unreachable
Request timeout
```

이 경우는 ROS2 문제보다 먼저 **네트워크 연결 또는 IP 설정 문제**를 확인해야 합니다.

---

## 2.2 ROS2 환경이 적용됐는지 확인

새로운 터미널을 열었다면 먼저 ROS2와 Unitree 작업환경을 적용합니다.

```bash
cd ~/Desktop/unitree_ros2

source /opt/ros/humble/setup.bash
source setup.sh
source install/setup.bash
```

RMW 설정을 확인합니다.

```bash
echo $RMW_IMPLEMENTATION
```

정상적으로 CycloneDDS를 사용하도록 설정되어 있다면 다음과 같이 출력됩니다.

```text
rmw_cyclonedds_cpp
```

> `source` 명령은 새로운 터미널을 열 때마다 다시 적용해야 할 수 있습니다.

---

## 2.3 DDS / RMW 확인

ROS2의 노드와 토픽은 **DDS**라는 통신 구조를 이용하여 서로를 발견하고 데이터를 주고받습니다.

간단히 표현하면 다음과 같습니다.

```text
ROS2 노드
→ RMW
→ DDS
→ 네트워크
→ 다른 ROS2 노드
```

- **RMW (ROS Middleware)**  
  ROS2가 어떤 DDS 구현체를 사용할지 연결해 주는 계층입니다.

- **DDS (Data Distribution Service)**  
  여러 ROS2 노드가 네트워크에서 서로를 발견하고 메시지를 주고받도록 하는 통신 시스템입니다.

Go2에서는 PC와 로봇이 서로 다른 장치이기 때문에 DDS가 올바른 네트워크 인터페이스를 사용하지 못하면 다음과 같은 상황이 발생할 수 있습니다.

```text
ping은 성공하지만

ros2 node list
ros2 topic list

에는 로봇의 노드나 토픽이 보이지 않음
```

먼저 RMW 설정을 확인합니다.

```bash
echo $RMW_IMPLEMENTATION
```

필요한 경우 다음과 같이 설정합니다.

```bash
export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
```

설정을 변경한 뒤 ROS2 daemon을 다시 시작합니다.

```bash
ros2 daemon stop
ros2 daemon start
```

---

## 2.4 노드가 보이는지 확인

현재 발견된 ROS2 노드를 확인합니다.

```bash
ros2 node list
```

Go2 관련 노드가 여러 개 보인다면 ROS2 Discovery가 이루어지고 있다는 뜻입니다.

네트워크 연결은 정상인데 로봇 관련 노드가 보이지 않는다면 다음 항목을 확인합니다.

```text
RMW 설정
CycloneDDS 네트워크 인터페이스 설정
ROS_DOMAIN_ID
```

Domain ID는 다음 명령으로 확인할 수 있습니다.

```bash
echo $ROS_DOMAIN_ID
```

서로 통신해야 하는 장치의 Domain ID가 다르면 같은 네트워크에 연결되어 있어도 ROS2 노드가 서로 발견되지 않을 수 있습니다.

> 아무 값도 출력되지 않는 경우에는 기본 Domain ID인 `0`을 사용하고 있을 가능성이 있습니다.

---

## 2.5 토픽이 보이는지 확인

현재 ROS2 네트워크에서 발견되는 토픽을 확인합니다.

```bash
ros2 topic list
```

Go2 관련 토픽이 보이는지 확인합니다.

예:

```text
/api/sport/request
/sportmodestate
/lidar_points
```

여기서 중요한 점은 **토픽 이름이 보인다고 해서 실제 데이터가 정상적으로 들어오고 있다는 뜻은 아니라는 것**입니다.

다음 단계에서 Publisher와 실제 데이터 발행 여부를 확인합니다.

---

## 2.6 Publisher / Subscriber 확인

특정 토픽의 연결 상태를 자세히 확인합니다.

예:

```bash
ros2 topic info /lidar_points -v
```

또는:

```bash
ros2 topic info /cmd_vel -v
```

출력에서 다음 항목을 확인합니다.

```text
Publisher count
Subscription count
```

### 예시 1 — `/lidar_points`

`Publisher count`가 `0`이라면:

```text
토픽 이름은 존재하지만
실제로 LiDAR 데이터를 발행하는 노드가 없음
```

이라는 뜻일 수 있습니다.

이 경우 LiDAR 드라이버가 정상적으로 실행되고 있는지 확인합니다.

---

### 예시 2 — `/cmd_vel`

`Publisher count`는 존재하지만 `Subscription count`가 `0`이라면:

```text
키보드 명령은 /cmd_vel로 보내고 있지만
그 명령을 받아서 Go2로 전달할 노드가 없음
```

이라는 뜻입니다.

이 경우 `go2_nav_bridge`와 같은 브리지 노드가 실행되고 있는지 확인합니다.

---

## 2.7 토픽 값이 실제로 들어오는지 확인

토픽에 실제 메시지가 들어오는지 확인합니다.

```bash
ros2 topic echo /토픽이름
```

예를 들어 `/cmd_vel`을 확인하려면:

```bash
ros2 topic echo /cmd_vel
```

키보드를 눌렀을 때 다음과 같은 값이 출력될 수 있습니다.

```yaml
linear:
  x: 0.5
angular:
  z: 0.0
```

이 값이 정상적으로 변한다면:

```text
키보드 입력
→ teleop 노드
→ /cmd_vel 발행
```

단계까지는 정상이라는 뜻입니다.

---

## 2.8 LiDAR 토픽 확인

LiDAR PointCloud 데이터는 양이 매우 많기 때문에 `ros2 topic echo`보다 주파수를 확인하는 것이 편합니다.

```bash
ros2 topic hz /lidar_points
```

다음과 같이 주파수 값이 계속 출력된다면 LiDAR 데이터가 정상적으로 발행되고 있다는 뜻입니다.

```text
average rate: ...
```

아무것도 출력되지 않는다면 다음 항목을 확인합니다.

```text
LiDAR 드라이버 실행 여부
센서 네트워크 연결
Publisher 실행 여부
QoS 설정
```

먼저 다음 명령으로 Publisher 상태와 QoS를 확인합니다.

```bash
ros2 topic info /lidar_points -v
```

---

# 3. Go2가 움직이지 않을 때 `/cmd_vel`부터 확인

키보드 Teleop을 실행했는데 Go2가 움직이지 않는다면 가장 먼저 `/cmd_vel`을 확인합니다.

```bash
ros2 topic echo /cmd_vel
```

키보드를 눌렀을 때 값이 정상적으로 나온다면:

```text
키보드 입력
→ /cmd_vel 발행
```

까지는 정상입니다.

그다음 브리지 노드가 실행 중인지 확인합니다.

```bash
ros2 node list
```

그리고 `/cmd_vel`의 Subscriber를 확인합니다.

```bash
ros2 topic info /cmd_vel -v
```

우리 실습 구조에서는 대략 다음과 같은 흐름으로 명령이 전달됩니다.

```text
teleop_twist_keyboard
        ↓
     /cmd_vel
        ↓
  go2_nav_bridge
        ↓
Unitree Sport API
        ↓
       Go2
```

따라서 `/cmd_vel` 값은 정상적으로 나오는데 로봇이 움직이지 않는다면 **`/cmd_vel` 이후의 브리지 또는 실제 로봇 연결 단계**를 확인해야 합니다.

---

# 4. QoS 확인

ROS2에서는 Publisher와 Subscriber가 데이터를 어떤 방식으로 주고받을지 **QoS(Quality of Service)**를 통해 설정합니다.

QoS는 쉽게 말하면:

> ROS2에서 메시지를 어떤 방식으로 전달하고 보관할지를 정하는 통신 규칙

입니다.

특정 토픽의 QoS는 다음 명령으로 확인할 수 있습니다.

```bash
ros2 topic info /lidar_points -v
```

예:

```text
QoS profile:
  Reliability: RELIABLE
  History (Depth): KEEP_LAST (1)
  Durability: VOLATILE
  Lifespan: Infinite
```

각 항목의 의미는 다음과 같습니다.

- **RELIABLE**  
  메시지가 가능한 한 정상적으로 전달되도록 재전송 등을 이용하는 방식입니다.

- **BEST_EFFORT**  
  메시지 전달을 최대한 시도하지만 손실된 메시지를 반드시 재전송하지는 않는 방식입니다.  
  LiDAR, 카메라와 같이 데이터가 빠르게 계속 들어오는 센서 토픽에서 자주 사용됩니다.

- **KEEP_LAST (1)**  
  가장 최근 메시지 `1개`만 보관합니다.

- **VOLATILE**  
  Subscriber가 연결되기 전에 지나간 메시지를 저장해 두었다가 새 Subscriber에게 다시 전달하지 않습니다.

- **Lifespan: Infinite**  
  메시지에 별도의 유효시간 제한이 설정되어 있지 않습니다.

Publisher와 Subscriber의 QoS가 호환되지 않으면:

```text
토픽 이름은 보이지만
실제 데이터는 수신되지 않는
```

상황이 발생할 수 있습니다.

---

## 4.1 BEST_EFFORT 토픽 확인

Publisher의 Reliability가 다음과 같이 표시된다면:

```text
Reliability: BEST_EFFORT
```

다음과 같이 확인할 수 있습니다.

```bash
ros2 topic echo /lidar_points --qos-reliability best_effort
```

---

## 4.2 RELIABLE 토픽 확인

Publisher의 Reliability가 다음과 같이 표시된다면:

```text
Reliability: RELIABLE
```

다음과 같이 확인할 수 있습니다.

```bash
ros2 topic echo /lidar_points --qos-reliability reliable
```

즉, 무조건 하나의 QoS 값을 사용하는 것이 아니라 먼저 다음 명령으로 Publisher의 QoS를 확인하는 것이 좋습니다.

```bash
ros2 topic info /lidar_points -v
```

---

# 5. 문제 발생 시 확인 순서

Go2 또는 ROS2 통신에 문제가 발생하면 아래 순서대로 확인합니다.

```text
1. ping 확인
   ↓
2. ROS2 환경 source 확인
   ↓
3. RMW / DDS 설정 확인
   ↓
4. ROS_DOMAIN_ID 확인
   ↓
5. ros2 node list
   ↓
6. ros2 topic list
   ↓
7. ros2 topic info <topic> -v
   ↓
8. ros2 topic echo 또는 ros2 topic hz
   ↓
9. Publisher / Subscriber / QoS 확인
   ↓
10. 브리지 → Sport API → 실제 Go2 단계 확인
```

핵심은 다음과 같습니다.

```text
어디까지 정상인지 확인하고
그다음 단계부터 문제를 찾는다.
```

예를 들어:

```text
ping 성공
+ node list 정상
+ topic list 정상
+ /cmd_vel 값 정상
+ /cmd_vel Subscriber 존재
```

한다면 키보드 입력이나 기본 ROS2 Discovery 문제보다는 **브리지 이후 또는 실제 Go2 제어 단계**를 확인하는 것이 효율적입니다.

반대로:

```text
ping부터 실패
```

한다면 ROS2 설정을 수정하기 전에 먼저 네트워크 연결부터 해결해야 합니다.
